[gd_scene load_steps=5 format=3 uid="uid://b2xq2h0qsxacd"]

[ext_resource type="Script" path="res://water_surface.gd" id="Script_wq1jx"]

[sub_resource type="Shader" id="Shader_wq1jx"]
code = "// Procedural noise-based water shader with interaction ripples
shader_type spatial;

uniform vec4 water_color : source_color = vec4(0.0, 0.42, 0.72, 0.88);
uniform vec4 deep_color : source_color = vec4(0.0, 0.1, 0.32, 1.0);
uniform float wave_speed : hint_range(0.0, 5.0) = 1.0;
uniform float wave_height : hint_range(0.0, 3.0) = 1.2;
uniform float fresnel_power : hint_range(0.1, 10.0) = 3.0;
uniform float specular_intensity : hint_range(0.0, 2.0) = 0.8;
uniform float metallic : hint_range(0.0, 1.0) = 0.1;
uniform float roughness : hint_range(0.0, 1.0) = 0.05;

// Ripple interaction — filled by GDScript
uniform vec4 ripple_data[16];
uniform float ripple_speed = 4.0;

varying vec3 world_pos;

vec3 mod289_v3(vec3 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }
vec2 mod289_v2(vec2 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }
vec3 permute(vec3 x) { return mod289_v3(((x * 34.0) + 1.0) * x); }

// 2D Simplex Noise — 使用三角形网格，天然无方形伪影
float snoise(vec2 v) {
    const vec4 C = vec4(0.211324865405187, 0.366025403784439,
                       -0.577350269189626, 0.024390243902439);
    vec2 i  = floor(v + dot(v, C.yy));
    vec2 x0 = v - i + dot(i, C.xx);
    vec2 i1 = (x0.x > x0.y) ? vec2(1.0, 0.0) : vec2(0.0, 1.0);
    vec4 x12 = x0.xyxy + C.xxzz;
    x12.xy -= i1;
    i = mod289_v2(i);
    vec3 p = permute(permute(i.y + vec3(0.0, i1.y, 1.0))
        + i.x + vec3(0.0, i1.x, 1.0));
    vec3 m = max(0.5 - vec3(dot(x0,x0), dot(x12.xy,x12.xy),
                            dot(x12.zw,x12.zw)), 0.0);
    m = m * m;
    m = m * m;
    vec3 x = 2.0 * fract(p * C.www) - 1.0;
    vec3 h = abs(x) - 0.5;
    vec3 ox = floor(x + 0.5);
    vec3 a0 = x - ox;
    m *= 1.79284291400159 - 0.85373472095314 * (a0*a0 + h*h);
    vec3 g;
    g.x  = a0.x  * x0.x  + h.x  * x0.y;
    g.yz = a0.yz * x12.xz + h.yz * x12.yw;
    return 130.0 * dot(m, g);
}

float fbm(vec2 p, int octaves) {
    float val = 0.0;
    float amp = 0.5;
    mat2 rot = mat2(vec2(0.866, 0.5), vec2(-0.5, 0.866));
    for (int i = 0; i < octaves; i++) {
        val += amp * (0.5 + 0.5 * snoise(p));
        p = rot * p * 2.03;
        amp *= 0.5;
    }
    return val;
}

float sample_noise_height(vec2 xz, float t) {
    vec2 p = xz * 0.08;
    float h = fbm(p + vec2(t * 0.04, t * 0.03), 4) * 0.6;
    h += fbm(p * 2.0 + vec2(t * -0.06, t * 0.05), 3) * 0.3;
    h += fbm(p * 5.0 + vec2(t * 0.1, t * -0.08), 2) * 0.1;
    // 高频碎波细节，让水面更细腻
    h += fbm(p * 12.0 + vec2(t * 0.15, t * -0.12), 2) * 0.03;
    // fbm 输出约 0~1，减去 0.5 使波浪以 0 为中心对称振荡
    return (h - 0.5) * wave_height;
}

float sample_ripple_height(vec2 xz) {
    float h = 0.0;
    for (int i = 0; i < 16; i++) {
        vec4 r = ripple_data[i];
        if (r.w <= 0.001) continue;
        vec2 center = r.xy;
        float elapsed = r.z;
        float intensity = r.w;
        float dist = length(xz - center);
        float radius = elapsed * ripple_speed;
        // 波纹环宽度随扩散自然增大，避免远处环太窄导致闪烁
        float ring_width = 2.0 + radius * 0.18;
        float d = abs(dist - radius);
        // 平滑过渡到 0，避免硬截断
        float ring_mask = 1.0 - smoothstep(0.0, ring_width, d);
        if (ring_mask <= 0.0) continue;
        float wave = sin(dist * 2.5 - elapsed * 8.0);
        h += wave * intensity * ring_mask * 0.2;
    }
    return h;
}

float sample_total_height(vec2 xz, float t) {
    return sample_noise_height(xz, t) + sample_ripple_height(xz);
}

void vertex() {
    world_pos = (MODEL_MATRIX * vec4(VERTEX, 1.0)).xyz;
    float t = TIME * wave_speed;
    float noise_h = sample_noise_height(world_pos.xz, t);
    float ripple_h = sample_ripple_height(world_pos.xz);
    VERTEX.y += noise_h + ripple_h;

    float e = 0.15;
    float hL = sample_total_height(world_pos.xz + vec2(-e, 0.0), t);
    float hR = sample_total_height(world_pos.xz + vec2( e, 0.0), t);
    float hU = sample_total_height(world_pos.xz + vec2(0.0, -e), t);
    float hD = sample_total_height(world_pos.xz + vec2(0.0,  e), t);
    NORMAL = normalize(vec3(hL - hR, e * 2.0, hU - hD));
}

void fragment() {
    vec3 view_dir = normalize(CAMERA_POSITION_WORLD - world_pos);
    float fresnel = pow(1.0 - max(dot(NORMAL, view_dir), 0.0), fresnel_power);
    vec4 final_color = mix(deep_color, water_color, fresnel);
    ALBEDO = final_color.rgb;
    ALPHA = final_color.a;
    METALLIC = metallic;
    ROUGHNESS = roughness;
    SPECULAR = specular_intensity;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_wq1jx"]
render_priority = 0
shader = SubResource("Shader_wq1jx")
shader_parameter/water_color = Color(0, 0.45, 0.75, 0.85)
shader_parameter/deep_color = Color(0, 0.12, 0.35, 1)
shader_parameter/wave_speed = 1.8
shader_parameter/wave_height = 1.2
shader_parameter/fresnel_power = 3.0
shader_parameter/specular_intensity = 0.8
shader_parameter/metallic = 0.1
shader_parameter/roughness = 0.05
shader_parameter/ripple_speed = 4.0

[sub_resource type="PlaneMesh" id="PlaneMesh_wq1jx"]
size = Vector2(100, 100)
subdivide_width = 256
subdivide_depth = 256

[sub_resource type="BoxShape3D" id="BoxShape3D_wq1jx"]
size = Vector3(100, 1, 100)

[node name="WaterSurface" type="MeshInstance3D"]
script = ExtResource("Script_wq1jx")
material_override = SubResource("ShaderMaterial_wq1jx")
mesh = SubResource("PlaneMesh_wq1jx")

[node name="Area3D" type="Area3D" parent="."]

[node name="CollisionShape3D" type="CollisionShape3D" parent="Area3D"]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.5, 0)
shape = SubResource("BoxShape3D_wq1jx")
